
  <tr>
    <td class ="myTitle" width="100%" colspan="2">
      <%= t('Editing ') + Configuration.human_name %>
    </td>                
  </tr> 
  <%= render 'form' %>
  <tr class="tablelinemain">
    <td colspan="2">
      <%= link_to t('Show'), @configuration %> |
      <%= link_to t('Back'), configurations_path %>
    </td>
  </tr>