
  <tr>
    <td class ="myTitle" width="100%" colspan="2">
      <%= t('Editing ') + Customer.human_name %>
    </td>
  </tr>
  <%= render 'form' %>
  <tr class="linemain">
    <td colspan="2">
      <%= link_to t('Show'), @customer %> |
      <%= link_to t('Back'), customers_path %>
    </td>
  </tr>

