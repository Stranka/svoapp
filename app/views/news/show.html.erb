  <tr>
    <td class ="myTitle" width="100%" colspan="2">
      <%= t('Show ') + News.human_name %>
    </td>                
  </tr> 
  <tr class="tablelinemain">
    <td colspan="2">
      <%= notice %>
    </td>
  </tr> 

  <tr class="tableline<%= cycle('1', '2') %>">
    <td width="20%">
  	 <%= News.human_attribute_name('title') %>
    </td>
    <td>
  	<%= @news.title %>
    </td>
  </tr>

  <tr class="tableline<%= cycle('1', '2') %>">
    <td width="20%">
  	 <%= News.human_attribute_name('date') %>
    </td>
    <td>
  	<%= @news.date %>
    </td>
  </tr>

  <tr class="tableline<%= cycle('1', '2') %>">
    <td width="20%">
  	 <%= News.human_attribute_name('notice') %>
    </td>
    <td>
  	<%= @news.notice %>
    </td>
  </tr>

  <tr class="tableline<%= cycle('1', '2') %>">
    <td width="20%">
  	 <%= News.human_attribute_name('format') %>
    </td>
    <td>
  	<%= @news.format %>
    </td>
  </tr>

  <tr class="tableline<%= cycle('1', '2') %>">
    <td width="20%">
  	 <%= News.human_attribute_name('auth_level') %>
    </td>
    <td>
  	<%= @news.auth_level %>
    </td>
  </tr>

  <tr class="tableline<%= cycle('1', '2') %>">
    <td width="20%">
  	 <%= News.human_attribute_name('auth_level_edit') %>
    </td>
    <td>
  	<%= @news.auth_level_edit %>
    </td>
  </tr>

  <tr class="tablelinemain">
    <td colspan="2">
	<%= link_to t('Edit'), edit_news_path(@news) %> |
	<%= link_to t('Back'), news_index_path %>
    </td>
  </tr>
