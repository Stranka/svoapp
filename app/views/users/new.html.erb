
  <tr>
    <td class ="myTitle" width="100%" colspan="2">
      <%= t('New ') + User.human_name %>
    </td>                
  </tr> 
<%= render 'form' %>

  <tr class="tablelinemain">
    <td colspan="2">
      <%= link_to t('Back'), users_path %>
    </td>
  </tr>