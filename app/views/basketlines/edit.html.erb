
  <tr>
    <td class ="myTitle" width="100%" colspan="2">
      <%= t('Editing ') + Basketline.human_name %>
    </td>
  </tr>
  <%= render 'form' %>
  <tr class="linemain">
    <td colspan="2">
      <%= link_to t('Show'), @basketline %> |
      <%= link_to t('Back'), basketlines_path %>
    </td>
  </tr>

