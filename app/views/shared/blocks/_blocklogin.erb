
  <%= form_for @user_session, :url => {:controller => 'user_sessions', :action => "create"} do |f| %>

      <%= link_to (image_tag('/images/at.gif')), :controller => 'menues', :action => 'change_language', :language => 'de' %>
      <%= link_to (image_tag('/images/en.gif')), :controller => 'menues', :action => 'change_language', :language => 'en' %>
      &nbsp;<%= t('Sprache wählen') %><br />
      <fieldset>
        <legend><%= t('login') %></legend>
        <p><%= f.label :login %> <%= f.text_field :login , :size => 20 %></p>
        <p><%= f.label :password %> <%= f.password_field :password, :size => 20 %></P>
        <%= f.submit t('login') %> &nbsp;
        <% if @config.shop_active == true %>
          <%= link_to t('New Customer'), :controller => :customers, :action => :new %>
        <% end %>
      </fieldset>
    <% end %>
