
  <tr>
    <td class ="myTitle" width="100%" colspan="2">
      <%= t('Editing ') + Basket.human_name %>
    </td>                
  </tr> 
  <%= render 'form' %>
  <tr class="tablelinemain">
    <td colspan="2">
      <%= link_to t('Show'), @basket %> |
      <%= link_to t('Back'), baskets_path %>
    </td>
  </tr>