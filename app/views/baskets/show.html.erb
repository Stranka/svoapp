  <tr>
    <td class ="myTitle" width="100%" colspan="2">
      <%= t('Show ') + Basket.human_name %>
    </td>
  </tr>
  <tr class="linemain">
    <td colspan="2">
      <%= notice %>
    </td>
  </tr>

  <tr class="line<%= cycle('1', '2') %>">
    <td width="20%">
      <%= Basket.human_attribute_name('session_id') %>
    </td>
    <td>
      <%= @basket.session_id %>
    </td>
  </tr>

  <tr class="line<%= cycle('1', '2') %>">
    <td width="20%">
      <%= Basket.human_attribute_name('customer_id') %>
    </td>
    <td>
      <%= @basket.customer_id %>
    </td>
  </tr>

  <tr class="line<%= cycle('1', '2') %>">
    <td width="20%">
      <%= Basket.human_attribute_name('status') %>
    </td>
    <td>
      <%= @basket.status %>
    </td>
  </tr>

  <tr class="line<%= cycle('1', '2') %>">
    <td width="20%">
      <%= Basket.human_attribute_name('shipmentterms') %>
    </td>
    <td>
      <%= @basket.shipmentterms %>
    </td>
  </tr>

  <tr class="line<%= cycle('1', '2') %>">
    <td width="20%">
      <%= Basket.human_attribute_name('paymentterms') %>
    </td>
    <td>
      <%= @basket.paymentterms %>
    </td>
  </tr>

  <tr class="line<%= cycle('1', '2') %>">
    <td width="20%">
      <%= Basket.human_attribute_name('order_number') %>
    </td>
    <td>
      <%= @basket.order_number %>
    </td>
  </tr>

  <tr class="line<%= cycle('1', '2') %>">
    <td width="20%">
      <%= Basket.human_attribute_name('store') %>
    </td>
    <td>
      <%= @basket.store %>
    </td>
  </tr>


  <tr class= "line<%= cycle('1', '2') %>">
  <td colspan="2">
  <table class="content">
  <tr class="lineheader">
    <td><%= Product.human_attribute_name('shorttext') %></td>
    <td><%= Basketline.human_attribute_name('quantity') %></td>
    <td><%= Basketline.human_attribute_name('value') %></td>
    <td><%= Basketline.human_attribute_name('price') %></td>
    <td><%= Basketline.human_attribute_name('tax_percentage') %></td>
    <td><%= Basketline.human_attribute_name('producer_number') %></td>
    <td><%= Basketline.human_attribute_name('weight') %></td>
  </tr>

  <% @basket.basketlines.each do |basketline| %>
    <tr class= "line<%= cycle('1', '2') %>">
      <td><%= basketline.product.shorttext %></td>
      <td><%= basketline.quantity %></td>
      <td><%= basketline.value %></td>
      <td><%= basketline.price %></td>
      <td><%= basketline.tax_percentage %></td>
      <td><%= basketline.producer_number %></td>
      <td><%= basketline.weight %></td>
    </tr>

  <% end %>
  </table>
  </td>
  </tr>

  <tr class="linemain">
    <td colspan="2">
  <%= link_to t('Edit'), edit_basket_path(@basket) %> |
  <%= link_to t('Back'), baskets_path %>
    </td>
  </tr>

